{{ include "simple-app.fullname" . }} has been deployed.

{{- if .Values.ingress.enabled }}
Access it via:
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}
{{- else if contains "ClusterIP" .Values.service.type }}
Access it via port-forward:
  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "simple-app.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}
{{- end }}
